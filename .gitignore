__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Configuracion real y datos generados. La barra inicial limita el patron a la
# raiz: sin ella tambien se ignorarian examples/inventory.csv y similares.
/config.yaml
/inventory.csv
/datos/
/respaldos/
*.log

# Estado del simulador de pruebas
tests/.dns_simulado

# Cosas propias del despliegue que no son del proyecto: la imagen de fondo del
# login, notas, pruebas. El panel la sirve desde donde diga web.fondo_login,
# asi que no tiene por que vivir en el repositorio.
/local/

# Ajustes de Claude Code, el asistente con el que se desarrolla esto. Ya no se
# suben: quien clona el repositorio viene a desplegar un respaldo de MikroTik, y
# encontrarse una carpeta de herramientas de desarrollo como primera linea del
# listado -antes que el codigo- es ruido en el sitio donde menos ayuda.
# Siguen existiendo en local, solo que fuera de git.
/.claude/
